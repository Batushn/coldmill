export async function openUrl(url: string) {
  console.info(`[preview] would open ${url}`);
}

export async function revealItemInDir(path: string) {
  console.info(`[preview] would reveal ${path}`);
}
